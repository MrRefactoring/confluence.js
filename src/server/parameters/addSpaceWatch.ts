export interface AddSpaceWatch {
  spaceKey: string;
  /** UserKey of the user to create the new watcher for */
  key?: string;
  /** UserName of the user to create the new watcher for */
  username?: string;
}

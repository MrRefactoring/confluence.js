export interface DeleteAttachmentPropertyById {
  /** The ID of the attachment the property belongs to. */
  attachmentId: string;
  /** The ID of the property to be deleted. */
  propertyId: number;
}

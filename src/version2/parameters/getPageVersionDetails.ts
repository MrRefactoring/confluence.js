export interface GetPageVersionDetails {
  /** The ID of the page for which version details should be returned. */
  id: number;
  /** The version number of the page to be returned. */
  versionNumber: number;
}

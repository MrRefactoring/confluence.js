export interface GetInlineCommentVersionDetails {
  /** The ID of the inline comment for which version details should be returned. */
  id: number;
  /** The version number of the inline comment to be returned. */
  versionNumber: number;
}

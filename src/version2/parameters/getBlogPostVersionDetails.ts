export interface GetBlogPostVersionDetails {
  /** The ID of the blog post for which version details should be returned. */
  blogpostId: number;
  /** The version number of the blog post to be returned. */
  versionNumber: number;
}
